{
  "name": "cs-web-server",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build-docker": "TAG=$(node -p \"require('./package.json').version\") docker compose build",
    "push-image": "TAG=$(node -p \"require('./package.json').version\") docker compose push",
    "serve": "vite preview"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "0.25.11",
    "jszip": "3.10.1",
    "typescript": "^5.9.3",
    "vite": "7.1.11",
    "xash3d-fwgs": "1.2.2"
  }
}
